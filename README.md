# ai-physics-Ly
## Environment Management
This project uses **Conda** to manage dependencies. The exact environment specifications are exported in `environment.yml` to ensure reproducibility across systems.
